# OnlineExam Frontend

Vue 3 + Vite + TypeScript frontend for the online exam system.

## Scripts

```bash
npm install
npm run dev
npm run build
```

The development server proxies `/api` requests to `http://127.0.0.1:8000` by default.
